{% comment %}
  Sección generada automáticamente para el componente React: ProductRating
  Arquitectura Híbrida: Liquid (SSR) + React Islands (Interactividad)
{% endcomment %}

{% schema %}
{
  "name": "Product Rating",
  "class": "productrating-section",
  "settings": [
    {
      "type": "header",
      "content": "Contenido"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Título de la sección",
      "default": "Product Rating"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Descripción"
    },
    {
      "type": "header",
      "content": "Configuración Interactiva"
    },
    {
      "type": "checkbox",
      "id": "enable_interaction",
      "label": "Habilitar interactividad",
      "default": true
    },
    {
      "type": "select",
      "id": "animation_speed",
      "label": "Velocidad de animación",
      "options": [
        { "value": "slow", "label": "Lenta" },
        { "value": "normal", "label": "Normal" },
        { "value": "fast", "label": "Rápida" }
      ],
      "default": "normal"
    }
  ]
}
{% endschema %}

{%- comment -%} Cargar CSS del componente React {%- endcomment -%}


<section class="productrating-section" id="section-{{ section.id }}">
  <div class="container">
    
    {%- comment -%} Header - Renderizado por Liquid (SSR) {%- endcomment -%}
    <div class="section-header">
      {%- if section.settings.title != blank -%}
        <h2 class="section-title">{{ section.settings.title | escape }}</h2>
      {%- endif -%}
      
      {%- if section.settings.description != blank -%}
        <div class="section-description">
          {{ section.settings.description | escape }}
        </div>
      {%- endif -%}
    </div>

    {%- comment -%} Contenido principal con React Island {%- endcomment -%}
    <div class="section-content">
            {%- comment -%} React Island: Componente Interactivo {%- endcomment -%}
      <react-component 
        name="ProductRating" 
        data-title="{{ section.settings.title | escape }}"
        data-description="{{ section.settings.description | escape }}"
        data-enable-interaction="{{ section.settings.enable_interaction }}"
        data-animation-speed="{{ section.settings.animation_speed }}">
        {%- comment -%} Fallback para usuarios sin JavaScript {%- endcomment -%}
        <div class="fallback-content">
          <p>{{ section.settings.title | default: 'ProductRating' }}</p>
          <p>Por favor, habilita JavaScript para una experiencia interactiva completa.</p>
        </div>
      </react-component>
    </div>
  </div>
</section>

{%- comment -%} Estilos CSS para la sección {%- endcomment -%}
<style>
  .productrating-section {
    padding: 3rem 0;
  }

  .productrating-section .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  .section-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .section-title {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: #333;
  }

  .section-description {
    font-size: 1.1rem;
    color: #666;
    max-width: 600px;
    margin: 0 auto;
  }

  .section-content {
    margin-top: 2rem;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .section-title {
      font-size: 2rem;
    }
    
    .productrating-section {
      padding: 2rem 0;
    }
  }
</style>

{%- comment -%}
  🎯 ARQUITECTURA HÍBRIDA:

  ✅ LIQUID (SSR) maneja:
  - Configuraciones de la sección
  - Datos de Shopify (productos, colecciones, etc.)
  - SEO y contenido inicial
  - URLs y navegación

  ✅ REACT (ProductRating) maneja:
  - Interactividad y estado
  - Eventos del usuario
  - Lógica compleja de UI
  - Progressive enhancement

  📝 Para personalizar:
  1. Edita assets/react-components/ProductRating.tsx
  2. Agrega estilos CSS si necesitas
  3. Ajusta esta sección según tus necesidades

  🚀 El componente React se hidrata automáticamente
     cuando la página carga usando React Islands.
{%- endcomment -%}